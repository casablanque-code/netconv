//! Этот крейт не содержит библиотечного кода.
//! Он существует, чтобы у workspace был отдельный, изолированный от
//! отдельных крейтов набор сквозных (end-to-end) тестов:
//! исходный конфиг IOS → парсер → IR → рендерер VRP/Eltex → проверка вывода.
//!
//! Зависимости от netconv-parser-ios / netconv-render-* объявлены как
//! dev-dependencies намеренно: ни один production-крейт (рендереры,
//! cli, wasm) не должен зависеть от парсера или друг от друга напрямую —
//! это сохраняет архитектурную границу IR-first дизайна. Тесты — единственное
//! законное место, где парсер и рендерер встречаются в одном крейте.
